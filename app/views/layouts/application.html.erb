<!DOCTYPE html>
<html>
<head>
  <title>Content-M&eacute;nage</title>
  <%= stylesheet_link_tag "reset", "layout", "typography", "jquery.fancybox-1.3.4", "formalize", "application" %>
  <%= javascript_include_tag "jquery.min", "rails", "jquery.formalize.min", "fancybox/jquery.fancybox-1.3.4.pack", "fancybox/jquery.easing-1.3.pack", "application" %>
  <%= csrf_meta_tag %>
</head>
<body>
  <header id="masthead">
    <h1><%= APP_TITLE %><% if @page_title %> &mdash; <%= @page_title %><% end %></h1>
    <nav>
      <ul class="no-bullets">
        <% if current_user %>
          <li class="<%= "active " if params[:controller] == "dashboard" %> first"><%= link_to "Dashboard", "/" %></li>
          <li><%= link_to "New content", new_content_path, :class => "fancybox" %></li>
        <% else %>
          <li<%= " class=\"active\"" if params[:controller] == "session"%>><%= link_to "Login", login_path %></li>
        <% end %>
      </ul>
    </nav>

  </header>
  <div id="wrapper" class="wrapper">
    <div id="inner-wrapper" class="inner-wrapper">
      <%= yield %>
    </div>
  </div>
</body>
</html>
